#ifndef ALU_H
#define ALU_H

#include <stdint.h>

uint16_t alu_add(uint8_t a, uint8_t b); //a and b are the two input variables that will be operated on
uint16_t alu_sub(uint8_t a, uint8_t b);

#endif
